import React from 'react';
import { renderWithWrapper } from '../test-utils';
import { Avatar } from '../index';

describe('Avatar', () => {
  it('test', () => {
    renderWithWrapper((
      <Avatar />
    ));
  });
});
