import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import { Button, PlusIcon, CloseIcon } from '../src';

<Meta
  title="Components/Button"
  component={Button}
/>

# Button

Buttons allow users to take actions, and make choices, with a single tap.

## Variant

The `Button` comes with three variants: `text` (default), `contained`, and `outlined`.

#### Text button

<Canvas>
<Button>Text</Button>
<Button disabled>Disabled</Button>
</Canvas>

#### Contained button

<Canvas>
<Button variant="contained">Contained</Button>
<Button variant="contained" disabled>Disabled</Button>
</Canvas>

#### Outlined button

<Canvas>
<Button variant="outlined">Outlined</Button>
<Button variant="outlined" disabled>Disabled</Button>
</Canvas>

## Size

Use the `size` prop to change the size of the button. You can set the value to `small`, `medium` (default) or `large`.

<Canvas>
<Button variant="contained" size="small">Small</Button>
<Button variant="contained" size="medium">Medium</Button>
<Button variant="contained" size="large">Large</Button>
</Canvas>

## Color

Use the `color` prop to change the color of the button. You can set the value to `default` (default), `primary`, `secondary`, `wrong` or `white`.

#### Default color

<Canvas>
<Button>Text</Button>
<Button variant="contained">
  Contained
</Button>
<Button variant="outlined">
  Outlined
</Button>
</Canvas>

#### Primary color

<Canvas>
<Button color="primary">Text</Button>
<Button color="primary" variant="contained">
  Contained
</Button>
<Button color="primary" variant="outlined">
  Outlined
</Button>
</Canvas>

#### Secondary color

<Canvas>
<Button color="secondary">Text</Button>
<Button color="secondary" variant="contained">
  Contained
</Button>
<Button color="secondary" variant="outlined">
  Outlined
</Button>
</Canvas>

#### Wrong color

<Canvas>
<Button color="wrong">Text</Button>
<Button color="wrong" variant="contained">
  Contained
</Button>
<Button color="wrong" variant="outlined">
  Outlined
</Button>
</Canvas>

#### White color

<Canvas
  style={{
    backgroundColor: 'var(--pv-color-black)',
  }}
>
<Button color="white">Text</Button>
<Button color="white" variant="contained">
  Contained
</Button>
<Button color="white" variant="outlined">
  Outlined
</Button>
</Canvas>

## Icon button

Sometimes you might want to have icons for certain buttons to enhance the UX of the application as we recognize logos more easily than plain text.

<Canvas>
<Button color="primary" variant="outlined" startIcon={<PlusIcon />}>
  Add
</Button>
<Button color="wrong" variant="contained" endIcon={<CloseIcon />}>
  Remove
</Button>
</Canvas>

## Props

<ArgsTable of={Button} />
