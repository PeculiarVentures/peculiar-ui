{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run --workspace @peculiar/react-components dev",
    "bootstrap": "npm install",
    "bootstrap:undo": "lerna clean --yes",
    "bootstrap:upgrade": "npm upgrade-interactive --latest",
    "clean": "lerna run --parallel clean",
    "build": "npm run build --workspaces",
    "test": "npm run test:unit:coverage --workspaces",
    "check:types": "npm run check:types --workspaces",
    "lint": "eslint",
    "prepare": "npm run build",
    "publish:version": "lerna publish",
    "publish:canary": "lerna publish --canary"
  },
  "devDependencies": {
    "@emotion/jest": "^11.13.0",
    "@peculiar/eslint-config-base": "^0.2.7",
    "@peculiar/eslint-config-react": "^0.2.7",
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^14.0.0",
    "@testing-library/user-event": "^14.4.3 ",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.19",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.0",
    "jest-watch-typeahead": "^2.2.2",
    "jsdom": "^24.1.1",
    "lerna": "^8.2.4",
    "ts-jest": "^29.2.4"
  },
  "license": "MIT",
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": ">=10",
      "onFail": "error"
    }
  }
}
